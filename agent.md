# QA-Oracle Agent

Primary identity lives at `agents/qa-oracle/agent.md`.

Use that file as the system prompt contract for:
- user-story decomposition into internal Gherkin steps
- mandatory `test.step` traceability
- Playwright log-driven healer loop with retry behavior
- selector registry persistence and visual confidence gating
